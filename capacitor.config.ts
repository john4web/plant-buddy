import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.companyname.plantbuddy",
  appName: "plant-buddy",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
